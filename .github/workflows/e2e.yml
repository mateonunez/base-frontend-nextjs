name: E2E Tests

on:
  push:
  pull_request:

jobs:
  cypress-test:
    runs-on: ubuntu-latest
    steps:
      - name: Github Checkout
        uses: actions/checkout@v2

      - name: Start APp
        run: |
          npm run build
          npm run start &

      - name: Run E2E
        run: |
          npm run cy:open
